angular.module('chriswebApp').directive('chrisSidebar', ['$window', function($window) {
	return {
		restrict: 'C',
		replace: true,
		scope: {
			
		},
		link: function(scope, element, attrs, fn) {
			
		}
	};
}]);